
// Write a program to print the sum of all the elements of an array of size N where N can be any integer between 1 and 100.
#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    int N,i;
    scanf("%d",&N);
    int numArray[N]; //define an array of n size
    for(i=0;i<N;i++)
    {
        scanf("%d",numArray);

    }
    int sum =N+i;
    printf("%d",sum);
}
